#include <stdio.h>

/**
 * main -Entry point
 *
 * Return: 0 always (success)
 *
 */

int main(void)

{
	char al;

	for (al = 'a'; al <= 'z'; al++;)

	{
		putchar(al);
	}

	{
		putchar('\n');
	}

return (0);
}
